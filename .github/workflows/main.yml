name: Build KarigAI APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      - name: Install Dependencies
        run: flutter pub get
        working-directory: karigai_backend/ml_models/models/colab_models/Secondary_Frontend
        
      - name: Build APK
        run: flutter build apk --release -t main.dart
        working-directory: karigai_backend/ml_models/models/colab_models/Secondary_Frontend
        
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: KarigAI-App-Release
          path: karigai_backend/ml_models/models/colab_models/Secondary_Frontend/build/app/outputs/flutter-apk/app-release.apk
